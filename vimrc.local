"" My customisations

if has("gui_running")
  if has("gui_mac") || has("gui_macvim")
    set guifont=SauceCodePro\ Nerd\ Font\ Mono:h16
    set transparency=1
  endif
endif

" NERDTree opens too wide
let g:NERDTreeWinSize=30
